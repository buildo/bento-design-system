import type { PatternResult, RuntimeFn, VariantGroups } from './types';
export declare const createRuntimeFn: <Variants extends VariantGroups>(config: PatternResult<Variants>) => RuntimeFn<Variants>;
