{
  "private": true,
  "scripts": {
    "dev": "nx run app:dev",
    "build": "nx run app:build",
    "generate:types": "nx run app:generate:types",
    "typecheck": "nx run-many --target=typecheck"
  },
  "devDependencies": {
    "nx": "^20.0.0"
  },
  "packageManager": "pnpm@9.7.0",
  "pnpm": {
    "patchedDependencies": {
      "remix-i18next": "patches/remix-i18next.patch"
    }
  }
}
